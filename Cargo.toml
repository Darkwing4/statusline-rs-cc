[package]
name = "statusline"
version = "0.3.0"
edition = "2021"

[dependencies]
serde_json = { version = "1", default-features = false, features = ["std"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
